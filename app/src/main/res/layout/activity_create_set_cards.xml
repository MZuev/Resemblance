<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_list_of_sets"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="ru.spbau.resemblance.ListOfSetsActivity">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:layout_centerHorizontal="true"
        android:id="@+id/createSetCardsLinearLayout">

        <Button
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            style="@android:style/Widget.Material.Light.Button.Borderless"
            android:text="Список наборов"
            android:id="@+id/ListOfSets"
            android:onClick="onListOfSetsClick"/>

        <Button
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            style="@android:style/Widget.Material.Light.Button.Borderless"
            android:text="Добавить набор"
            android:id="@+id/AddSetsCards"
            android:onClick="onAddSetsCardsClick"/>

        <Button
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            style="@android:style/Widget.Material.Light.Button.Borderless"
            android:text="Список карт"
            android:id="@+id/ListOfCards"
            android:onClick="onListOfCardsClick"/>

        <Button
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            style="@android:style/Widget.Material.Light.Button.Borderless"
            android:text="Добавить карты"
            android:id="@+id/onAddCardsClick"
            android:onClick="onAddCardsClick"/>

    </LinearLayout>

</RelativeLayout>
